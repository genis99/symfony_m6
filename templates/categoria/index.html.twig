{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Categories</h1>
    <p>Si s'intenten duplicar les categories donarà errorde unique contraint!</p>

    {% for categoria in categories %}
        <div style="display: flex; justify-content: space-between;">
            <div>{{ categoria.name }}</div>
            <div style="margin-top: 1em;">{{ include('categoria/_delete_form.html.twig') }}</div>
        </div>
    {% endfor %}
    <a href="{{ path('nova_categoria') }}"><button>Nova categoria</button></a>
    <div style="margin-top: 1em;"><a href="{{ path('canals') }}">Tornar a canals</a></div>
</div>
{% endblock %}
